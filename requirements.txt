# ============================================================
# Logistics Cargo Damage Detection System — Dependencies
# ============================================================
# Core CV / DL
ultralytics>=8.1.0          # YOLOv8 engine (training, inference, tracking)
opencv-python>=4.8.0        # Video I/O and frame processing
torch>=2.1.0                # PyTorch backend (CUDA wheels installed separately)

# Utilities
numpy>=1.24.0               # Numerical operations
pyyaml>=6.0                 # YAML config parsing
tqdm>=4.65.0                # Progress bars

# Optional — quality of life
matplotlib>=3.7.0           # Result visualisation & training curves
Pillow>=10.0.0              # Image format support
lap>=0.4.0                  # Linear Assignment Problem solver (ByteTrack dep)

# Dashboard
streamlit>=1.30.0            # Interactive web dashboard
plotly>=5.18.0               # Rich interactive charts
pandas>=2.0.0                # Data manipulation (used by dashboard)
